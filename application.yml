spring:
  profiles:
    active:
      - dev

---

spring:
  profiles: dev #开发环境
  application:
    name: MicroServiceCloud-Config-Dev

---

spring:
  profiles: test #测试环境
  application:
    name: MicroServiceCloud-Config-Test
